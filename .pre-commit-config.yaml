repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        entry: uv run ruff format --check
        language: system
        types_or: [python, pyi]
        pass_filenames: true

      - id: ruff-check
        name: ruff check
        entry: uv run ruff check --fix
        language: system
        types_or: [python, pyi]
        pass_filenames: true

      - id: ty
        name: ty check
        entry: uv run ty check packages apps/tablesage-tui
        language: system
        pass_filenames: false

      - id: ty-optimization
        name: ty check prompt optimization
        entry: uv run --project apps/optimize-prompts ty check --python apps/optimize-prompts/.venv apps/optimize-prompts
        language: system
        files: ^apps/optimize-prompts/
        pass_filenames: false

      - id: pytest
        name: pytest
        # Parallel (pytest-xdist): the Textual tests mostly wait on the pilot's idle polling, so one worker per
        # core cuts the suite from ~2.5 minutes to ~20 seconds.
        entry: uv run pytest -n auto
        language: system
        pass_filenames: false
        stages: [pre-push]

      - id: pytest-optimization
        name: pytest prompt optimization
        entry: uv run --project apps/optimize-prompts pytest apps/optimize-prompts/tests
        language: system
        files: ^apps/optimize-prompts/
        pass_filenames: false
        stages: [pre-push]
