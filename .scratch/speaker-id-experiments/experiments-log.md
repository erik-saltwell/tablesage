# Speaker identification — experiments log

Tracks attempts to improve speaker identification quality, scored against the
[speaker identification benchmark harness](../../.documentation/speaker_identification_benchmark.md).

Status is one of:

- **Unstarted** — idea captured, no design doc yet.
- **Designed** — has a dedicated design doc, not yet run.
- **Tried** — run against the benchmark harness; result recorded below.

Result type (populated once Status is Tried):

- **Success** — improved benchmark scores enough to adopt.
- **Failure** — did not improve scores, or regressed something; not adopted.

| # | Idea | Status | Result type | Result explanation | Design doc |
| --- | --- | --- | --- | --- | --- |
| 1 | Update similarity threshold based on results sweep | Tried | Success | Swept `similarity_margin_threshold` 0.00–0.50 (then 0.00–0.02 at step 0.001) against both frozen sessions. Original default (0.1) scored 0.760 pooled; headline score plateaus at 0.834–0.839 across 0.007–0.02. Adopted `0.07` (score 0.797, error 2.3%) instead of the plateau peak, trading headline score for a lower error rate closer to the original's 0.9%. Applied to both `settings.yaml` copies and `candidates.py`'s `production` baseline — currently optimal: 0.07. | [`01-similarity-threshold-sweep.md`](01-similarity-threshold-sweep.md) |
| 2 | Swap embedder to SpeechBrain ECAPA-TDNN (`speechbrain/spkrec-ecapa-voxceleb`, Apache-2.0, VoxCeleb1+2-trained) — current `eres2netv2` is Mandarin-trained and used off-domain on English speech; ECAPA-TDNN is trained on VoxCeleb (English-heavy) and widely cited around ~1.7% EER on VoxCeleb1-O | Tried | Success | At the same threshold (0.07) as `production`, `ecapa-tdnn` scores **0.884** pooled vs. `production`'s 0.797 (+0.087, +10.9% relative), holding on both sessions individually. Accuracy **76.0%** (vs. 52.7%), unassigned **20.5%** (vs. 45.0%), error **3.4%** (vs. 2.3%). Bigger lever than experiment #1's threshold tuning. Not yet adopted — threshold was held at eres2netv2's tuned value, not swept for this embedder; production-side (`tablesage-tools`) swap is separate follow-up work. | — (implementation shelved, `git stash list` on `refactor`) |
| 3 | Swap embedder to WeSpeaker ResNet34-LM (`Wespeaker/wespeaker-voxceleb-resnet34-LM`, CC-BY-4.0, VoxCeleb2-trained) — the same checkpoint family pyannote.audio's diarization pipeline uses in production, so a proven fit for this exact diarize-then-identify workflow (loaded via the raw HF repo + `wespeaker-unofficial`, not the `pyannote/...` wrapper — `pyannote.audio`'s own model loading is broken in this environment, torchaudio 2.10 removed an API it depends on) | Tried | Success | At the same threshold (0.07) as `production`, `wespeaker-resnet34` scores **0.902** pooled vs. `production`'s 0.797 (+0.105, +13.2% relative) — the best result so far, ahead of experiment #2's `ecapa-tdnn` (0.884). Holds on both sessions individually. Accuracy **79.9%** (vs. 52.7%), unassigned **17.1%** (vs. 45.0%), error **3.0%** (vs. 2.3%). **Adopted in production** (see experiment #5's threshold-tuned value) — `tablesage_tools.embeddings.wespeaker.EmbeddingFactory` now backs `identify_speakers`/centroid computation, replacing `eres2netv2` (kept as `benchmarks/speaker_id/candidates.py`'s `eres2netv2-baseline` for historical comparison). | [`03-wespeaker-resnet34-embedder.md`](03-wespeaker-resnet34-embedder.md) |
| 4 | Swap embedder to NVIDIA NeMo TitaNet-Large (`nvidia/speakerverification_en_titanet_large`, CC-BY-4.0, English-trained) — cited around ~1.9% EER, among the lowest of the three candidates, but heavier dependency (`nemo_toolkit[asr]`) | Tried | Success | At the same threshold (0.07) as `production`, `titanet-large` scores **0.906** pooled vs. `production`'s 0.797 (+0.109, +13.7% relative) — the best result across all four experiments, narrowly ahead of experiment #3's `wespeaker-resnet34` (0.902, a ~statistical tie at this sample size). Holds on both sessions individually. Accuracy **81.1%** (vs. 52.7%), unassigned **16.0%** (vs. 45.0%), error **3.0%** (vs. 2.3%). `nemo_toolkit[asr]` needed a real dependency-conflict workaround (`onnx<1.18` pin, not just an environment shim). Not yet adopted — threshold held at eres2netv2's tuned value; production-side (`tablesage-tools`) swap and weighing this dependency's weight against `wespeaker-resnet34`'s much lighter one are separate follow-up work. | — (implementation shelved, `git stash list` on `refactor`) |
| 5 | Sweep `similarity_margin_threshold` per-embedder for the two tied leaders (`wespeaker-resnet34`, `titanet-large`) to see if tuning reorders their near-tied experiment #3/#4 scores, both measured at 0.07 (tuned for `eres2netv2`, not either of these) | Tried | Success | Swept 0.00–0.40 per embedder. `wespeaker-resnet34` tuned peak: **0.904** at threshold 0.08 (vs. 0.902 at the shared 0.07) — accuracy 79.5%, unassigned 18.3%, error 2.3%. `titanet-large` tuned peak: **0.906** at threshold 0.07 (already optimal, unchanged) — accuracy 81.1%, unassigned 16.0%, error 3.0%. Gap narrows from 0.004 to 0.002 — tuning tightens the tie rather than resolving it; both curves are far flatter than eres2netv2's (experiment #1's 0.760→0.839 swing). **Adopted**: `wespeaker-resnet34` at `similarity_margin_threshold: 0.08` — statistically tied with `titanet-large` but far lighter to deploy (no `nemo_toolkit[asr]`/`onnx` conflict). Applied to both `settings.yaml` copies and production's real embedder (`tablesage_tools.embeddings.wespeaker`), not just the benchmark harness — currently optimal: `wespeaker-resnet34` @ 0.08. | [`05-threshold-sweep-leaders.md`](05-threshold-sweep-leaders.md) |
| 6 | Two-pass speaker ID: refine each speaker's centroid mid-session from pass 1's own high-confidence utterances (count-weighted blend with the reference-clip centroid), then rescue pass 1's `UNASSIGNED_SPEAKER` utterances against the refined centroids in a second pass — targets the ~17–18% of utterances still landing in the unassigned gray zone with `wespeaker-resnet34` @ 0.08, using session-matched audio instead of cross-session reference clips | Tried | Success | At initial knobs (`k=1`) barely moved the needle (0.904→0.905 pooled) — one session showed zero change at all. Diagnostic: well-enrolled players (100–250 ref. clips) get only 3–23% session weight at `k=1`, so one session's utterances can't move their centroid much; only a severely under-enrolled player (`jeff devries`, 14 clips) got meaningfully refined. Swept `k` (1→128): accuracy climbs 79.5%→82.4%, unassigned drops 18.3%→13.9%, but headline score barely moves (0.904→0.908) since ~1/3 of rescued utterances land wrong. Per-session at `k=128`: `20260818-end` improves cleanly (error rate unchanged); `20260825-end` (contains `jeff devries`) scores *worse* than production despite higher accuracy — error rate more than doubles, and 3 of 4 new confusion pairs involve `jeff devries` specifically. The circularity risk flagged at design time materialized concretely, concentrated on the one under-enrolled speaker. Small real gain, not adopted — secondary/pass-2 thresholds still unswept, and under-enrolled speakers likely need their own guard. | [`06-two-pass-centroid-refinement.md`](06-two-pass-centroid-refinement.md) |
| 7 | Richer decision rule: use absolute best similarity and utterance duration alongside the current best-vs-runner-up margin, so strong or sufficiently long evidence can use a different abstention bar than short/noisy evidence | Tried | Success | Swept **840** combinations over base margin (0.06–0.10), absolute-best OR-gate (off or 0.30–0.45), duration cutoff (off or 1.0–3.0s), and duration margin (0.00–0.06), reusing cached WeSpeaker embeddings. Pooled maximum: **0.911** at `margin≥0.10 OR best≥0.375`, with margin 0 for ≥1s, but error rises 2.3%→3.0% and misattributed audio 9.0s→17.4s. The robust/maximin winner is simpler: **margin≥0.10 for <1s; margin≥0.04 for ≥1s** (no absolute-similarity gate), scoring **0.908** vs. 0.904, improving both sessions, accuracy 79.5%→80.1%, unassigned 18.3%→17.8%, and error 2.3%→2.1%. **Adopted in production**: `identify_speakers` now applies the duration-conditioned threshold, with both thresholds and the 1.0s cutoff read from `speaker_identification` settings; the absolute-similarity gate remains benchmark-only because it added errors. | [`07-richer-decision-rule.md`](07-richer-decision-rule.md) |
| 8 | Diarization-cluster propagation: label ElevenLabs diarization clusters from pooled WeSpeaker evidence, then propagate the cluster label to short/unassigned utterances with a per-utterance contradiction veto | Tried | Success | The fixture prerequisite was already present: corrected names are on each utterance while original cluster IDs remain on its words. Cluster majority purity is **92.8%** and **83.1%** by utterance (97.9%/91.5% duration-weighted), so unconstrained propagation is risky. Swept **6,272** combinations across evidence duration, uniform/duration pooling, independent/one-to-one mapping, cluster margin, propagation scope/duration, and veto margin. Pooled maximum: **0.931** vs. production 0.908, accuracy 80.1%→91.3%, but error 2.1%→5.7% (9→25 wrong) — not recommended. Conservative winner: independently label clusters from ≥0.5s utterances, propagate only production-unassigned utterances ≤0.5s, veto when the utterance favors another player by margin ≥0.02. It scores **0.919**, accuracy **83.1%**, unassigned **14.6%**, error **2.3%**: +13 correct, −14 unassigned, +1 wrong (only 0.20s), improving both sessions; session one adds zero errors. **Adopted in production** with experiment #9 after the combined experiment #12 scored 0.925. The experimental implementation remains shelved as `experiment-8 diarization cluster propagation`. | [`08-diarization-cluster-propagation.md`](08-diarization-cluster-propagation.md) |
| 9 | Short-utterance embedding widening: for utterances under ~1s, merge adjacent audio from the *same diarized cluster* before embedding, excluding wall-clock gaps and intervening speakers | Tried | Success | Swept **72** combinations of short cutoff, widened speech target, neighbor-gap cap, and original-vs-widened decision duration. Headline maximum scores **0.918** vs. production 0.908 and raises accuracy 80.1%→84.2%, but adds five net wrong labels (error 2.1%→3.2%); do not adopt it. Conservative winner widens only utterances <0.75s to 1.0s from same-cluster spans within 2s and retains the original-duration threshold. It scores **0.916**, improves both sessions, adds nine correct labels, removes nine abstentions, and holds wrong labels at 9 (2.1%); misattributed audio rises only 0.21s because two old errors are replaced by two new ones. WeSpeaker returned non-finite embeddings through 0.10s; 0.125s was finite but weak, so retain `MIN_UTTERANCE_DURATION_SECONDS=0.15`. **Adopted in production** with experiment #8 after the combined experiment #12 scored 0.925; fixed duration-bucket reporting was also added to the permanent harness. The experimental implementation remains shelved as `experiment-9 short-utterance embedding widening`. | [`09-short-utterance-embedding-widening.md`](09-short-utterance-embedding-widening.md) |
| 10 | Multi-prototype references: replace each player's single mean centroid with 2–4 spherical k-means sub-centroids, score max-over-prototypes, and audit the reference outlier-trim bar | Tried | Failure | Swept **3,984** configurations over 1–4 prototypes, outlier trimming off/0.4–0.8, and retuned short/long margins. The three-prototype maximum scores **0.914** vs. production 0.908 and raises accuracy 80.1%→82.9%, but adds four wrong labels and slightly regresses session one. A four-prototype low-error candidate scores 0.912 with 7 vs. 9 wrong labels, but regresses session one 0.917→0.912; **no multi-prototype configuration** held wrong labels at or below production while matching/improving both session scores. Do not adopt. Separate audit: the 0.6 trim bar removes 131/775 unique clips, including 75/229 for Jason. Lowering it to 0.5 with the existing single centroid/rule scores 0.910, turns one wrong label into an abstention, and does not regress either session, but the +0.0014 gain is too small for adoption from two fixtures; validate on more sessions. Implementation shelved as `experiment-10 multi-prototype references`. | [`10-multi-prototype-references.md`](10-multi-prototype-references.md) |
| 11 | Adaptive score normalization (AS-Norm): normalize each utterance/player cosine score from enrollment- and test-side top-N statistics against a held-out imposter cohort | Tried | Failure | Built a **200-speaker held-out LibriSpeech cohort** (two clips averaged per speaker) and swept **15,624** combinations of cohort size, top-N, mean centering, and normalized short/long margins. Headline maximum scores **0.914** vs. production 0.908, but adds two wrong labels and 5.92s misattributed audio. A conservative 100-speaker/top-50 candidate scores **0.911**, improves both sessions, and adds three correct labels at the same 9 wrong labels, but swaps in one longer new error. With the full 200-speaker cohort, the best safe result (0.911, 8 wrong) uses all 200 scores—S-Norm, not adaptive top-N; the best safe true AS-Norm result scores only 0.909 and improves one label. **Do not adopt:** the small gain is sensitive to cohort composition and does not justify a new resource/score scale without more frozen sessions and an in-domain cohort. Implementation and 1.12 MB cohort resource shelved as `experiment-11 adaptive score normalization`. | [`11-adaptive-score-normalization.md`](11-adaptive-score-normalization.md) |
| 12 | Compose the conservative winners from experiments #8 and #9 in the real production path, then rerun the frozen-session benchmark against pre-8/9 production | Tried | Success | New production scores **0.925** vs. 0.908: accuracy 80.1%→84.7%, unassigned 17.8%→13.0%, error 2.1%→2.3%, and counts 351/78/9→371/57/10. It turns 19 abstentions correct and three wrong, while correcting one old error and turning another into an abstention: +20 correct, −21 unassigned, +1 wrong, +0.41s misattributed audio. Both sessions improve (0.917→0.947 and 0.901→0.908), though both net new errors are concentrated in session two. All changed outcomes are under 0.75s. The three new errors already occur in the standalone winners (one from #8, two from #9), so composition creates no novel wrong label. **Adopted in production**, with all algorithm knobs in settings and diagnostics for monitoring/rollback. | [`12-production-composition-8-9.md`](12-production-composition-8-9.md) |
