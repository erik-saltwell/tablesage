# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ty/

# Virtual environments
.venv/
venv/
env/

# Build artifacts
build/
dist/
*.egg-info/

# Coverage
.coverage
htmlcov/

# Environment files
.env

# OS/editor
.DS_Store
Thumbs.db

# VS Code local settings are usually okay to commit selectively.
# Keep .vscode/settings.json if it contains shared project settings.

dev.db
.tablesage/

# Downloaded ML model checkpoints (e.g. MossFormer2 voice enhancement) --
# fetched at runtime, not source.
checkpoints/

# Speaker-ID benchmark harness's on-disk embedding cache -- derived, not source.
# benchmarks/data/ (the frozen session fixtures) is intentionally NOT ignored.
benchmarks/.cache/
